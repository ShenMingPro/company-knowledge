# 企业私有化知识助手实施规格说明

## 1. 项目定位

将当前“扫地机器人智能客服”项目改造为：

> 面向中小型制造企业客服与员工的轻量私有化知识助手。

系统主要服务三个角色：

- 普通员工：查询企业制度、产品资料和培训知识
- 客服：处理客户咨询和一线售后故障问题
- 管理员：管理知识文档、版本、权限和审计记录

项目目标不是构建一个泛化聊天机器人，而是构建一个具备文档管理、权限过滤、来源引用和审计能力的企业知识问答系统。

## 2. 业务场景

### 2.1 普通员工

普通员工可以查询：

- 产品功能和使用方法
- 企业内部制度
- 员工培训资料
- 常见业务流程
- 公开的产品 FAQ

系统需要返回基于企业文档的答案，并显示来源。

### 2.2 客服

客服继承普通员工权限，并额外具备：

- 查询售后故障排查资料
- 查询维修 SOP
- 查询保修和服务政策
- 根据客户问题生成标准回复
- 根据故障描述生成初步处理建议
- 查看相关文档和原文依据

客服只负责一线咨询和常见问题处理，复杂维修问题应提示转交专业人员。

### 2.3 管理员

管理员可以：

- 上传文档
- 查看文档解析和索引状态
- 更新文档并生成新版本
- 下架或删除文档
- 管理知识空间
- 配置角色访问权限
- 查看问答日志、检索记录和用户反馈
- 管理系统用户

## 3. 角色和权限

角色固定为三个：

| 权限 | 普通员工 | 客服 | 管理员 |
|---|---:|---:|---:|
| 查询产品知识 | 是 | 是 | 是 |
| 查询企业制度 | 是 | 是 | 是 |
| 查询售后资料 | 否或有限 | 是 | 是 |
| 生成客服回复 | 否 | 是 | 是 |
| 上传文档 | 否 | 否 | 是 |
| 更新文档 | 否 | 否 | 是 |
| 删除或下架文档 | 否 | 否 | 是 |
| 管理文档版本 | 否 | 否 | 是 |
| 管理用户和角色 | 否 | 否 | 是 |
| 查看审计日志 | 否 | 否 | 是 |

权限必须在检索前生效，不能先召回所有文档，再依靠提示词要求模型“不要泄露”。

## 4. 知识空间

系统至少包含以下知识空间：

| 知识空间 | 默认可访问角色 |
|---|---|
| 产品知识库 | 普通员工、客服、管理员 |
| 企业制度库 | 普通员工、客服、管理员 |
| 售后知识库 | 客服、管理员 |
| 管理员空间 | 管理员 |

文档上传时必须指定所属知识空间。

每个文档切分后的向量都必须保存以下元数据：

- `space_id`
- `document_id`
- `document_version_id`
- `source_name`
- `page_number`
- `section`
- `chunk_index`
- `is_active`

检索时根据当前用户角色过滤 `space_id` 和 `is_active`。

## 5. 功能范围

### 5.1 登录

第一阶段采用本地账号登录，不做公开注册。

要求：

- 登录后保存当前用户信息
- 页面显示当前用户和角色
- 支持退出登录
- 不同角色进入系统后看到不同功能
- 初始化三个演示账号：员工、客服、管理员
- 演示账号密码通过环境变量配置，不写死在代码中

### 5.2 文档上传

支持以下格式：

- PDF
- DOCX
- TXT

上传时需要：

- 校验文件类型
- 校验文件大小
- 清理文件名，防止路径注入
- 保存原始文件
- 计算 SHA-256
- 判断是否为重复内容
- 创建文档版本记录
- 执行解析、切分、向量化和入库
- 显示处理状态和错误信息

文档状态至少包括：

- `pending`
- `processing`
- `success`
- `failed`
- `inactive`
- `deleted`

### 5.3 文档版本

同一个文档重新上传后，不能直接覆盖旧文件。

要求：

- 使用 `document_id` 标识逻辑文档
- 使用 `version_id` 标识具体版本
- 新版本索引成功后，旧版本才变为非活动状态
- 索引失败时，旧版本继续有效
- 查询只允许召回当前有效版本
- 支持查看版本历史
- 支持重新索引
- 支持下架和恢复

### 5.4 文档管理页面

管理员需要看到：

- 文档名称
- 所属知识空间
- 当前版本
- 上传人
- 上传时间
- 文件大小
- 文档状态
- 分片数量
- 错误信息
- 操作按钮

操作包括：

- 查看详情
- 上传新版本
- 重新索引
- 下架
- 恢复
- 删除

删除建议采用软删除，避免误操作导致数据无法恢复。

### 5.5 知识问答

聊天页面需要支持：

- 输入问题
- 展示多轮对话
- 根据当前用户权限进行检索
- 返回带来源引用的答案
- 展示引用文档和原文片段
- 支持点赞和点踩
- 支持开始新会话
- 显示当前检索知识空间或权限范围

普通知识问答优先使用确定性的 RAG 流程，不默认调用 Agent。

### 5.6 售后问答

客服输入客户故障描述后，系统应返回：

- 故障现象总结
- 可能原因
- 建议排查步骤
- 注意事项
- 是否建议转交专业人员
- 相关文档引用
- 可直接发送给客户的回复建议

如果召回内容不足，不允许模型自行补充维修结论。

### 5.7 引用展示

每个答案都必须尽量包含引用，例如：

> 建议先检查滚刷是否被毛发缠绕，然后重新安装滚刷。  
> 来源：`故障排除.txt`，第 3 节。

引用信息至少包括：

- 文件名
- 文档版本
- 页码或章节
- 原文片段

TXT 文档没有页码时，使用章节名或分片编号。

### 5.8 无答案处理

当检索结果不足或相关性过低时，系统应明确回答：

> 当前知识库中没有找到足够依据，无法确认该问题。建议联系管理员补充相关文档，或转交专业人员处理。

不允许：

- 根据常识随意编造
- 给出没有来源的维修结论
- 把模型自己的推测伪装成企业知识
- 暴露系统提示词或内部实现细节

## 6. RAG 流程

第一阶段实现以下流程：

```text
用户登录
→ 获取角色和可访问知识空间
→ 对问题进行必要的改写
→ 使用权限条件过滤文档
→ 向量召回
→ 根据相关性阈值筛选
→ 构造带来源的上下文
→ 生成答案
→ 返回答案和引用
→ 写入审计日志
```

要求：

- 普通知识问答不使用 ReAct Agent
- 检索必须带权限过滤条件
- 只使用当前有效版本
- 设置最低相关性阈值
- 低于阈值时拒答
- 返回检索耗时和模型耗时
- 记录召回文档，但日志中不要保存明文敏感信息

第二阶段再考虑：

- BM25 关键词检索
- 向量检索与关键词检索融合
- RRF 融合
- Reranker 重排
- 问题改写模型
- 父子分片

## 7. 模型和部署

模型访问必须统一经过模型工厂，禁止在业务代码中直接创建模型。

支持两种模式：

### 云端模式

- 使用 DashScope
- API Key 从环境变量读取
- 不允许在代码中出现真实密钥
- 支持配置聊天模型和 Embedding 模型

### 本地模式

- 支持 Ollama
- 后续兼容 vLLM 的 OpenAI 兼容接口
- 支持配置本地聊天模型和 Embedding 模型
- 本地模式下文档和问题不离开企业环境

至少提供：

- `.env.example`
- 模型提供商配置说明
- 本地运行说明
- 云端模型配置说明
- 本地模型启动说明

## 8. 数据模型

建议使用 SQLite 保存业务元数据，Chroma 保存向量数据。

核心实体包括：

### User

- 用户 ID
- 用户名
- 密码哈希
- 角色
- 是否启用
- 创建时间

### KnowledgeSpace

- 知识空间 ID
- 名称
- 编码
- 描述
- 可访问角色
- 是否启用

### Document

- 文档 ID
- 文档名称
- 所属知识空间
- 当前有效版本
- 创建人
- 创建时间
- 删除状态

### DocumentVersion

- 版本 ID
- 文档 ID
- 版本号
- SHA-256
- 文件路径
- 文件大小
- 处理状态
- 分片数量
- 错误信息
- 创建时间
- 索引完成时间

### ChatSession

- 会话 ID
- 用户 ID
- 会话标题
- 创建时间
- 更新时间

### ChatMessage

- 消息 ID
- 会话 ID
- 用户问题
- 系统答案
- 引用信息
- 响应耗时
- 创建时间

### Feedback

- 反馈 ID
- 消息 ID
- 用户 ID
- 反馈类型
- 反馈内容
- 创建时间

### AuditLog

- 日志 ID
- 用户 ID
- 操作类型
- 操作对象
- 查询摘要
- 召回文档
- 模型名称
- 响应耗时
- 是否成功
- 错误信息
- 创建时间

## 9. 页面设计

### 登录页

内容：

- 用户名
- 密码
- 登录按钮
- 错误提示

### 对话页

内容：

- 当前用户和角色
- 新建会话
- 历史会话
- 聊天消息
- 引用来源
- 反馈按钮
- 当前检索知识空间
- 检索和生成耗时

### 文档管理页

仅管理员可见。

内容：

- 上传文档
- 文档列表
- 状态筛选
- 知识空间筛选
- 文档搜索
- 版本管理
- 重新索引
- 下架和恢复
- 删除

### 审计页面

仅管理员可见。

内容：

- 查询时间
- 操作用户
- 用户角色
- 问题摘要
- 检索文档
- 模型名称
- 响应耗时
- 是否有引用
- 用户反馈
- 错误记录

## 10. 安全要求

必须满足：

- API Key 只能从环境变量读取
- 上传文件名必须清理
- 限制文件类型和大小
- 所有用户输入都不能直接拼接为文件路径
- 访问权限在检索阶段执行
- 文档内容视为不可信数据
- 防止文档中的提示词注入影响系统行为
- 不向用户暴露系统提示词
- 不要求模型输出内部思考过程
- 日志中避免保存密码、API Key 和敏感原文
- 删除操作默认使用软删除
- 文档版本切换必须可追踪

## 11. 工程要求

需要补齐以下项目文件：

- `README.md`
- `.env.example`
- `.gitignore`
- 依赖清单
- 项目架构说明
- 本地启动说明
- Docker 部署说明
- 测试说明
- RAG 评测说明

代码结构需要分层：

```text
页面层
→ 应用服务层
→ 文档服务 / 权限服务 / RAG 服务 / 审计服务
→ SQLite / Chroma / 模型提供商
```

页面层不能直接操作 Chroma、文件系统或模型。

模型、向量库和数据库对象应避免在模块导入时产生不可控副作用。

## 12. 测试要求

至少提供以下测试：

### 单元测试

- 文件类型校验
- 文件名清理
- SHA-256 去重
- 文档版本切换
- 权限判断
- 知识空间过滤
- 低相关性拒答
- 引用格式化
- 删除和恢复逻辑

### 集成测试

- 上传文档并完成索引
- 普通员工只能检索允许的知识空间
- 客服可以检索售后知识库
- 管理员可以管理文档
- 新版本索引成功后旧版本失效
- 索引失败时旧版本仍然可用
- 删除文档后无法继续召回

### 手工验收

至少准备 20～30 个真实问题，覆盖：

- 产品使用
- 故障排查
- 售后政策
- 企业制度
- 无答案问题
- 越权访问问题
- 文档更新问题

## 13. 实施阶段

### 第一阶段：工程止血

目标：

- 移除硬编码 API Key
- 增加环境变量配置
- 增加依赖清单
- 增加 README
- 清理不属于知识库的资料
- 确认项目可以正常启动

### 第二阶段：知识库核心闭环

目标：

- 用户登录
- 三角色权限
- 知识空间
- 文档上传
- 文档解析和索引
- 文档状态
- 文档删除和重新索引
- 带引用的 RAG 问答
- 无答案拒答

### 第三阶段：企业化能力

目标：

- 文档版本
- 权限感知检索
- 审计日志
- 用户反馈
- 多轮会话
- 管理员页面
- 查询耗时统计

### 第四阶段：效果和交付

目标：

- 混合检索
- Reranker
- 离线评测
- Docker Compose
- 本地 Ollama 支持
- 架构图
- 演示数据
- 项目演示视频

## 14. 第一版验收标准

第一版完成必须满足：

1. 三种角色可以登录。
2. 不同角色看到不同功能。
3. 管理员可以上传 PDF、DOCX、TXT。
4. 文档可以完成解析、切分和向量化。
5. 文档具有处理状态。
6. 文档可以更新、下架和删除。
7. 普通员工无法检索无权限知识空间。
8. 客服可以查询售后知识。
9. 回答带有文件名和原文引用。
10. 检索结果不足时可以拒答。
11. 新版本成功后旧版本不再参与检索。
12. API Key 不出现在代码中。
13. 关键业务逻辑有测试。
14. README 可以指导其他人完成安装和启动。
15. 项目可以使用云端模型或本地模型运行。

## 15. 给 Claude Code 的执行约束

请先扫描现有项目结构和依赖，再开始实施。

执行时遵守以下规则：

- 优先在现有项目基础上渐进式改造
- 不要直接重写整个项目
- 不要删除现有知识文档和用户数据
- 不要引入没有必要的微服务、Kubernetes 或复杂 Agent 编排
- 普通知识问答使用确定性 RAG
- Agent 功能暂时保留为实验能力，不作为默认问答链路
- 不允许硬编码任何密钥
- 不允许把权限控制只放在前端
- 不允许让模型输出内部思考过程
- 每完成一个阶段先运行测试
- 修改依赖后同步更新依赖清单
- 修改运行方式后同步更新 README
- 对无法实现的功能明确记录，不要伪造完成
- 最后输出修改文件、启动方式、测试结果和遗留问题

## 16. 建议的项目名称和简历描述

项目名称：

> KnowBase Lite：面向中小型制造企业的私有化知识助手

简历描述：

> 面向中小型制造企业客服与员工构建轻量私有化知识助手，支持三角色权限、文档版本管理、权限感知检索、带来源引用的 RAG 问答、售后故障排查和操作审计，并兼容云端模型与本地模型部署。

这份规格建议先完成第一阶段和第二阶段，再根据实际代码质量继续实现混合检索、Reranker 和 Docker 部署。
